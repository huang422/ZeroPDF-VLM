# Document Template Alignment & ROI Extraction
# Python 3.9+

# Core dependencies
opencv-python>=4.8.0
numpy>=1.24.0
PyMuPDF>=1.23.0

# VLM Recognition via Ollama glm-ocr
requests>=2.28.0
Pillow>=10.0.0
opencc-python-reimplemented>=0.1.7

# Hardware detection (optional - for GPU auto-detection without torch)
# torch is optional: only used for GPU VRAM detection, not for model inference
# If torch is not installed, GPU detection falls back to nvidia-smi

# Development dependencies (optional)
pytest>=7.4.0
